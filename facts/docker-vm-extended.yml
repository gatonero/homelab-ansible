hostname: docker-vm
os: Debian 13.3
kernel: 6.12.63+deb13-amd64

ssh:
  password_authentication: "no"
  permit_root_login: "key-only"

ntp:
  synchronized: "no"

qemu_guest_agent:
  present: True
  active: active
